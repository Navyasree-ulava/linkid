services:
  postgres:
    image: postgres:16
    container_name: linkid-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: linkid
    ports:
      - "5432:5432"